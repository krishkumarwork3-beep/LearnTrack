package com.learntrack.service;

/**
 * Tests for PriorityQueueService.
 * Implementation Plan reference: section 5 (queue build, important_flag filtering).
 *
 * Suggested cases:
 *  - a topic with important_flag = 0 never enters the built queue
 *  - topics, skills, and eligible PYQ items all normalize into one ordering
 *  - queue re-push after partial allocation keeps the item's reduced remaining value
 */
public class PriorityQueueServiceTest {
}
